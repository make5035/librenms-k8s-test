# Wave 1. MODÈLE EN CLAIR — chiffrer avec SOPS+age AVANT commit, ne jamais
# committer en clair. Le manifest réel committé est k8s/10-secret.sops.yaml.
apiVersion: v1
kind: Secret
metadata:
  name: librenms-secrets
  namespace: librenms
  annotations:
    argocd.argoproj.io/sync-wave: "1"
type: Opaque
stringData:
  DB_USER: "librenms"
  DB_PASSWORD: "CHANGER_MOI"
  APP_KEY: "base64:REPORTER_APP_KEY_PRODUCTION"
  SNMP_COMMUNITY: "CHANGER_MOI"
