# librenms.env — env_file injecté dans les conteneurs. Copier en `librenms.env`,
# NE PAS committer (gitignoré).
# APP_KEY vide => générée au 1er boot et persistée dans ./data/librenms.
# Pour importer une base de production chiffrée, y reporter l'APP_KEY de prod.
APP_KEY=
