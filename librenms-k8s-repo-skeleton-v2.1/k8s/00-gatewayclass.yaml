# Wave 0. Cilium 1.15.x NE crée PAS la GatewayClass automatiquement : la déclarer
# explicitement (sinon k get gatewayclass -> No resources found, fiche #18).
apiVersion: gateway.networking.k8s.io/v1
kind: GatewayClass
metadata:
  name: cilium
  annotations:
    argocd.argoproj.io/sync-wave: "0"
spec:
  controllerName: io.cilium/gateway-controller
